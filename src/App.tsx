import { Home } from "./pages/Home";

function App() {
  return <Home />;
}

export default App;

//Tailwind e prettier Tailwind
//-> Tailwind é um framework de css baseado em classes utilitárias de propósito único (atomic css);

//Atomic Design;
//Composite Pattern;
//React hook form;
//zod;
//useReducer;
//components;
//type vs interface (no typescript);
